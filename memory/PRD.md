# NT - نظام نقاط البيع

## الوصف
نظام POS متكامل لإدارة المخزون والمبيعات والمشتريات باللغتين العربية والفرنسية

---

## التحديثات الأخيرة (2026-02-09) - الجلسة الحالية

### 20. كود البيع التلقائي في نقطة البيع ✅ جديد
تم إضافة كود البيع التلقائي بصيغة `BV0001/26`:
- يظهر الكود في أعلى صفحة نقطة البيع
- يتم جلب كود جديد عند كل عملية بيع جديدة
- يُرسل مع بيانات البيع للـ Backend

### 21. اختصارات المنتجات السريعة ✅ جديد
تم إضافة 20 مربع اختصار في أسفل صفحة نقطة البيع:
- **الموقع:** أسفل صفحة نقطة البيع
- **عدد المربعات:** 20 مربع (10 في كل صف)
- **الميزات:**
  - اختيار منتج لكل مربع
  - اختيار لون مخصص لكل مربع
  - حفظ الإعدادات في localStorage
  - النقر على المربع يضيف المنتج للسلة
  - النقر بالزر الأيمن للتعديل

### 22. عناوين القائمة الجانبية بخط خشن ✅ جديد
تم تحديث أسلوب عناوين الأقسام في القائمة الجانبية:
- **من:** `text-xs font-semibold text-muted-foreground`
- **إلى:** `text-sm font-bold text-foreground`

### 23. الباركود في وصل البيع ✅ جديد
تم إضافة حقل الباركود لكل منتج في بيانات البيع:
- يُرسل الباركود مع كل منتج في السلة
- يمكن طباعته على الوصل

---

### 18. إضافة أكواد تلقائية للحصص اليومية والجرد ✅ جديد
تم إضافة أكواد تلقائية مع عرضها في حوارات البدء:

**الصيغ المستخدمة:**
- حصص البيع اليومية: `S001/26` (S + 3 أرقام + /YY)
- جرد المخزون: `IN0001/26` (IN + 4 أرقام + /YY)
- المشتريات: `AC0001/26` (AC + 4 أرقام + /YY)

**الملفات المُحدّثة:**
- `DailySessionsPage.js` - إضافة حالة `sessionCode` وعرضها في حوار فتح الحصة
- `InventoryCountPage.js` - إضافة حالة `inventoryCode` وعرضها في حوار بدء الجرد

### 19. صفحة تعديل المنتج تتضمن OCR ✅ موجود
صفحة تعديل المنتج تحتوي على ميزة استخراج الموديلات المتوافقة من الصورة (OCR)

**الميزات:**
- زر "استخراج من صورة" بجانب حقل الموديلات المتوافقة
- يدعم صيغ JPEG, PNG, WebP
- يضيف الموديلات المستخرجة إلى الحقل تلقائياً

---

### 13. تحديث صفحة تعديل المنتج ✅ جديد
تم تحديث صفحة تعديل المنتج لتتطابق مع صفحة إضافة منتج جديد:

**الميزات الجديدة:**
- زر رفع الصورة (Camera icon) بدلاً من حقل URL فقط
- اختيار العائلة (Family select dropdown) مع إمكانية الإضافة السريعة
- استخراج الموديلات من الصورة (OCR)
- نفس تصميم وترتيب الحقول في صفحة الإضافة

### 14. زر تصدير Excel السريع ✅ جديد
تم إضافة زر Excel سريع في صفحة المنتجات:

**الموقع:** بجانب زر "إضافة منتج" في رأس الصفحة
**الوظيفة:** تصدير جميع المنتجات إلى ملف Excel بضغطة واحدة

### 15. قسم الفوائد الشهرية ✅ جديد
تم إضافة قسم حساب الأرباح الشهرية في لوحة التحكم:

**المعادلة:** صافي الربح = المبيعات - تكلفة الشراء - التكاليف

**البطاقات الأربعة:**
1. المبيعات الشهرية (أخضر)
2. تكلفة الشراء (أزرق)
3. التكاليف الشهرية (أحمر)
4. صافي الربح (أخضر/أحمر حسب القيمة)

**API الجديد:** `/api/dashboard/profit-stats`

### 16. توحيد الواجهة لجميع المستخدمين ✅ جديد
تم إزالة القيود على القائمة الجانبية لتكون موحدة لجميع المستخدمين:

**العناصر المتاحة للجميع الآن:**
- المخزون: المنتجات، عائلات المنتجات، المخازن، الجرد، طباعة الباركود، تحديث الأسعار، سجل الأسعار
- المبيعات: نقطة البيع، الجلسات اليومية، المبيعات، التقارير المتقدمة
- الإدارة: جميع العناصر متاحة للجميع

### 17. تحويل الأرقام إلى التنسيق العادي ✅ جديد
تم تحديث تنسيق التاريخ والوقت لاستخدام الأرقام العادية (0-9) بدلاً من الأرقام العربية (٠-٩)

**الملفات المُحدّثة:**
- SalesHistoryPage.js
- PriceHistoryPage.js
- UsersPage.js
- SalesHistoryDialog.js
- CustomersPage.js

**الأداة الجديدة:** `/frontend/src/utils/dateFormat.js`

---

### 11. أزرار الطباعة والتصدير الشاملة ✅ جديد
تم إضافة مكون `ExportPrintButtons` إلى جميع الصفحات الرئيسية:

**الصفحات المُحدّثة:**
- صفحة المنتجات (ProductsPage) ✅ موجود سابقاً
- صفحة الزبائن (CustomersPage) ✅ جديد
- صفحة الموردين (SuppliersPage) ✅ جديد
- صفحة التكاليف (ExpensesPage) ✅ جديد
- صفحة المبيعات (SalesHistoryPage) ✅ جديد
- صفحة المشتريات (PurchasesPage) ✅ جديد

**الوظائف المتاحة:**
- طباعة (Print) - يفتح نافذة الطباعة
- تصدير إلى Excel (.xls)
- تصدير إلى CSV
- تصدير إلى PDF

**المكون:** `/frontend/src/components/ExportPrintButtons.js`

### 12. Custom Hook لماسح الباركود ✅ جديد
تم إنشاء hook قابل لإعادة الاستخدام لدعم ماسح الباركود:

**الملف:** `/frontend/src/hooks/useBarcodeScanner.js`

**الاستخدام:**
```javascript
const { handleKeyDown } = useBarcodeScanner((barcode) => {
  // Handle scanned barcode
  searchProducts(barcode);
}, { playSound: true });
```

**الخيارات:**
- `playSound`: تشغيل صوت عند المسح (افتراضي: true)
- `minLength`: الحد الأدنى لطول الباركود (افتراضي: 3)
- `maxTime`: أقصى وقت بين الضغطات (افتراضي: 100ms)
- `enabled`: تفعيل/تعطيل المسح (افتراضي: true)

---

## المهام المعلقة

### P1 - أولوية متوسطة
- ⏳ تعميم البحث بالكود لجميع الكيانات (العملاء، الموردين، المشتريات، المصاريف)
- ⏳ تعميم دعم ماسح الباركود لجميع حقول البحث باستخدام useBarcodeScanner hook
- ⏳ إنهاء إعادة هيكلة منطق الديون (نقل من المبيعات إلى العملاء)

### P2 - أولوية منخفضة
- إضافة الترميز التلقائي للكيانات المتبقية (MT, IN, BV, S)
- التحقق من إزالة حقل الاسم العربي من جميع الصفحات
- اختبار صوت الباركود على المتصفحات المختلفة

---

## التحديثات السابقة (2026-02-09)

### 6. كود المنتج التلقائي (Article Code) ✅ جديد
تمت إضافة حقل "كود المنتج" مع توليد تلقائي بصيغة AR00001, AR00002, ...

**المميزات:**
- يظهر تحت/بجانب اسم المنتج في صفحات الإضافة والتعديل
- توليد تلقائي عند فتح صفحة إضافة منتج جديد
- الصيغة: AR + 5 أرقام (AR00001, AR00002, ...)
- الكود للقراءة فقط (لا يمكن تعديله)
- يزيد تلقائياً مع كل منتج جديد
- يظهر في قائمة المنتجات (list view)

### 7. نظام الأكواد التلقائية لجميع القطاعات ✅ جديد
تم إضافة أكواد تلقائية لجميع القطاعات الرئيسية:

| القطاع | الصيغة | مثال |
|--------|--------|------|
| المنتجات | AR + 5 أرقام | AR00001 |
| الزبائن | CL + 5 أرقام | CL00001 |
| الموردين | FR + 5 أرقام/السنة | FR00001/2026 |
| المبيعات | BV + 5 أرقام/السنة | BV00001/2026 |
| المشتريات | AC + 5 أرقام/السنة | AC00001/2026 |
| التكاليف | CH + 5 أرقام/السنة | CH00001/2026 |
| الجرد | IN + 5 أرقام/السنة | IN00001/2026 |
| تحديث الأسعار | MT + 5 أرقام/السنة | MT00001/2026 |
| حصة البيع | S + 4 أرقام/السنة | S0001/2026 |

**APIs الجديدة:**
- `GET /api/products/generate-article-code`
- `GET /api/customers/generate-code`
- `GET /api/suppliers/generate-code`
- `GET /api/sales/generate-code`
- `GET /api/purchases/generate-code`
- `GET /api/expenses/generate-code`
- `GET /api/inventory-sessions/generate-code`
- `GET /api/price-updates/generate-code`
- `GET /api/daily-sessions/generate-code`

### 8. البحث بالكود وقارئ الباركود ✅ جديد
تم تفعيل البحث بالكود في جميع أشرطة البحث ودعم قارئ الباركود:

**أشرطة البحث المُحدّثة:**
- شريط البحث الرئيسي (Header)
- نقطة البيع (POSPage)
- طباعة الباركود (BarcodePrintPage)
- تحديث الأسعار (BulkPriceUpdatePage)
- الجرد (InventoryCountPage)
- المشتريات (PurchasesPage)
- مكون ProductSearchDropdown

**دعم قارئ الباركود:**
- الضغط على Enter بعد المسح يضيف المنتج مباشرة للسلة
- يبحث بالباركود وكود المنتج
- يدعم جميع أنواع القارئات (USB, Bluetooth)

**ربط الباركود بكود المنتج:**
- عند توليد باركود جديد، يُبنى على كود المنتج
- الصيغة: 213 (الجزائر) + 0001 (الشركة) + كود المنتج (5 أرقام) + رقم التحقق
- مثال: AR00004 → 2130001000049

### 9. صوت التنبيه عند مسح الباركود ✅ جديد
تمت إضافة صوت تنبيه عند مسح الباركود بنجاح:

**الملف:** `/frontend/src/utils/beep.js`

**الأصوات:**
- `playSuccessBeep()`: صوت نجاح عند إضافة منتج (1000Hz)
- `playErrorBeep()`: صوت خطأ عند عدم العثور (300Hz)

**الأماكن المُفعّلة:**
- نقطة البيع (POSPage)
- مكون ProductSearchDropdown

### 10. كود الشراء التلقائي ✅ جديد
- الصيغة: AC00001/2026
- يظهر في أعلى نموذج الشراء
- كود المنتج يظهر بجانب كل منتج

### 5. التصميم المضغوط لصفحات الإضافة ✅ 
تم تطبيق تصميم مضغوط على جميع نماذج الإضافة لتقليل التمرير وتحسين تجربة المستخدم:

#### صفحة إضافة/تعديل الزبون:
- **الصف 1**: 3 أعمدة (الاسم، الهاتف، البريد)
- **الصف 2**: 4 أعمدة (التصنيف، الخصم، حد الدين، الميلاد)
- **الصف 3**: 3 أعمدة (رقم الهوية، السجل التجاري، العائلة)
- **الصف 4**: 2 عمودين (العنوان، الملاحظات)
- حقول أصغر (h-9)، تسميات أصغر (text-xs)

#### صفحة إضافة/تعديل المورد:
- **الصف 1**: 2 أعمدة (الاسم، الهاتف)
- **الصف 2**: 2 أعمدة (البريد، العائلة)
- حقول العنوان والملاحظات كاملة العرض

#### صفحة إضافة المصاريف:
- **الصف 1**: 2 أعمدة (الفئة، الوصف)
- **الصف 2**: 2 أعمدة (المبلغ، التاريخ)
- خيارات التكرار مدمجة بشكل مضغوط

### 5. مفتاح حساب متوسط سعر الشراء ✅ جديد
- أُضيف إلى صفحة **إضافة المنتج** وصفحة **تعديل المنتج**
- أيقونة الآلة الحاسبة بجانب حقل سعر الشراء
- Tooltip يوضح الوظيفة (حساب السعر المتوسط)
- يُحفظ مع بيانات المنتج في الحقل `use_average_price`

---

## التحديثات السابقة (2026-02-09)

### 1. نظام تحديثات النظام (للسوبر أدمين) ✅ جديد
- **صفحة مخصصة** `/system-updates` للسوبر أدمين فقط
- **إرسال إعلانات** لجميع المشتركين مع:
  - العنوان والرسالة (عربي/فرنسي)
  - أنواع متعددة (معلومات، ميزة جديدة، صيانة، تحذير، عرض خاص)
  - مستويات الأولوية (منخفضة، عادية، عالية، عاجلة)
  - استهداف (الجميع أو النشطين فقط)
- **نشر الإعدادات** لجميع المشتركين:
  - إعدادات الإيصال
  - إعدادات الإشعارات
  - إعدادات الولاء
  - إعدادات نقطة البيع
- **إحصائيات** (إجمالي المشتركين، النشطين، الإعلانات، إشعارات اليوم)

### 2. الآلة الحاسبة في نقطة البيع ✅ جديد
- زر برتقالي في الزاوية السفلية اليسرى
- عمليات حسابية كاملة (+، -، ×، ÷)
- وظائف إضافية (%, √, CE, C, ±)
- تصميم عصري ومتجاوب

### 3. إعادة هيكلة الشريط الجانبي ✅
- نقل "المستخدمين" و"الصلاحيات" إلى قسم "إدارة الموظفين"
- إنشاء قسم "الإعدادات" منفصل
- قسم "الإدارة" يحتوي فقط على "مميزات البرنامج"

---

## الميزات المنجزة

### إدارة المنتجات ✅
- عرض وإضافة وتعديل المنتجات
- توليد الباركود تلقائياً
- عائلات المنتجات
- تحديث الأسعار الجماعي
- **نشر المنتجات على WooCommerce** (جديد)

### نقطة البيع (POS) ✅
- البيع النقدي والدين والجزئي
- خدمة التوصيل لجميع ولايات الجزائر
- طباعة الفواتير

### إدارة صيانة الهواتف ✅ (جديد)
- تتبع طلبات الصيانة (مستلم، قيد الفحص، قيد الإصلاح، جاهز، مسلم)
- استقبال أجهزة جديدة مع كل التفاصيل
- إدارة مخزون قطع الغيار
- **ربط قطع الغيار مع المخزون الرئيسي** (جديد)
- خصم تلقائي من المخزون عند استخدام القطع
- **إشعارات WhatsApp للعملاء** (جديد) - يتطلب إعداد WhatsApp Business API

### تطبيق PWA ✅ (جديد)
- أيقونة تثبيت التطبيق في الهيدر
- manifest.json للتثبيت على الهواتف
- Service Worker للعمل دون إنترنت
- أيقونات مخصصة (192x192 و 512x512)

### إدارة المشتريات ✅
- شراء من الموردين
- الدفع نقداً أو بالدين أو جزئي
- تتبع حسابات الموردين
- تسديد ديون الموردين

### إدارة المال ✅
- الصندوق النقدي
- الحساب البنكي
- المحفظة الإلكترونية
- الخزنة
- تحويل الأموال بين الصناديق

### حصص البيع اليومية ✅
- كل موظف يفتح حصته الخاصة
- فتح حصة يومية مع رصيد افتتاحي
- تتبع المبيعات النقدية وبالدين
- غلق الحصة مع المبلغ الفعلي في الصندوق
- حساب الفرق (فائض/عجز)
- تبويب "حصصي" - حصص المستخدم الحالي
- تبويب "الكل" - جميع حصص الموظفين (للمسؤول)
- تبويب "التقارير" - ملخص إجمالي + تقرير لكل موظف

### عائلات الزبائن والموردين ✅
- تصنيف الزبائن والموردين في مجموعات
- إضافة/تعديل/حذف العائلات
- عرض عدد الأعضاء في كل عائلة
- البحث في العائلات

### إدارة المخزون ✅
- المخازن المتعددة
- نقل المخزون بين المخازن
- جرد المخزون
- طباعة الباركود (عدة أشكال)

### نظام الإشعارات ✅
- تنبيهات المخزون المنخفض
- تذكيرات الديون المستحقة
- إشعارات العجز/الفائض في الصندوق
- الحد قابل للتخصيص من الإعدادات

### الإعدادات ✅
- صلاحيات المستخدمين (admin, manager, user)
- تغيير كلمات مرور المستخدمين (للمسؤول)
- إعدادات الطابعة (UI فقط)
- إعدادات عامة (حدود التنبيهات)
- **تخصيص صفحة تسجيل الدخول** (شعار، اسم، صورة خلفية) - جديد

## الميزات الجديدة (2026-02-07)

### 1. تخصيص صفحة تسجيل الدخول ✅
- جلب إعدادات العلامة التجارية من `/api/branding/settings`
- دعم تخصيص الشعار والاسم وصورة الخلفية
- الإعدادات متاحة من صفحة الإعدادات للمسؤول

### 2. إعادة تصميم واجهة نقطة البيع (POS) ✅ - جديد
- **التصميم الجديد (2026-02-07):**
  - خلفية بيضاء/فاتحة نظيفة
  - **قائمة بحث منسدلة** للمنتجات تظهر عند الكتابة
  - القائمة تتقلص أثناء الكتابة وتصفي النتائج
  - النقر على المنتج يضيفه للسلة ويغلق القائمة
  - جدول للسلة مع أزرار الكمية (+/-)
  - شريط سفلي للإجماليات وأزرار الدفع
  - مكون `ProductSearchDropdown` قابل لإعادة الاستخدام

### 3. الإحصائيات المتقدمة ✅
- صفحة جديدة `/analytics`
- رسم بياني لتطور المبيعات (أسبوع/شهر/سنة)
- توقعات المبيعات للأسبوع القادم
- أفضل المنتجات مبيعاً
- أفضل العملاء
- أداء الموظفين
- منتجات تحتاج إعادة طلب

### 3. نظام الولاء والتسويق ✅
- صفحة جديدة `/loyalty`
- **إعدادات برنامج الولاء:**
  - تفعيل/تعطيل البرنامج
  - نقاط لكل دينار
  - قيمة النقطة
  - الحد الأدنى للاستبدال
  - مكافأة الترحيب
- **إدارة نقاط العملاء**
- **حملات SMS** (وضع المحاكاة)
  - إنشاء حملات جديدة
  - استهداف (الكل، المديونين، غير النشطين)
  - جدولة الحملات

### 4. نشر على WooCommerce ✅
- زر "نشر على WooCommerce" في صفحة تفاصيل المنتج
- زر "إلغاء النشر" للمنتجات المنشورة
- عرض حالة النشر وتاريخ آخر مزامنة
- **ملاحظة:** API في وضع المحاكاة

## صفحات النظام
| المسار | الوصف | الصلاحية |
|--------|-------|----------|
| `/` | لوحة التحكم | الكل |
| `/pos` | نقطة البيع | الكل |
| `/products` | المنتجات | الكل |
| `/products/add` | إضافة منتج | Admin |
| `/products/:id` | تفاصيل المنتج (مع زر WooCommerce) | الكل |
| `/sales` | المبيعات | الكل |
| `/purchases` | المشتريات | Admin |
| `/daily-sessions` | حصص البيع اليومية | Admin |
| `/customers` | الزبائن | الكل |
| `/customer-families` | عائلات الزبائن | Admin |
| `/suppliers` | الموردين | Admin |
| `/supplier-families` | عائلات الموردين | Admin |
| `/warehouses` | المخازن | Admin |
| `/inventory-count` | جرد المخزون | Admin |
| `/barcode-print` | طباعة الباركود | Admin |
| `/cash` | إدارة المال | Admin |
| `/reports` | التقارير | Admin |
| `/analytics` | الإحصائيات المتقدمة | Admin |
| `/loyalty` | الولاء والتسويق | Admin |
| `/woocommerce` | إعدادات WooCommerce | Admin |
| `/shipping` | شركات الشحن | Admin |
| `/sim-management` | إدارة الشرائح | Admin |
| `/settings` | الإعدادات | Admin |

## APIs الجديدة

### تخصيص صفحة الدخول
```
GET /api/branding/settings (public)
PUT /api/branding/settings (admin)
```

### الإحصائيات المتقدمة
```
GET /api/analytics/sales-chart?period=week|month|year
GET /api/analytics/top-products?limit=10
GET /api/analytics/top-customers?limit=10
GET /api/analytics/employee-performance
GET /api/analytics/sales-prediction
GET /api/analytics/restock-suggestions
```

### نظام الولاء
```
GET  /api/loyalty/settings
PUT  /api/loyalty/settings
GET  /api/loyalty/customer/:id
POST /api/loyalty/earn
POST /api/loyalty/redeem
```

### حملات SMS
```
GET  /api/marketing/sms/campaigns
POST /api/marketing/sms/campaigns (MOCKED)
```

### WooCommerce
```
GET    /api/woocommerce/settings
PUT    /api/woocommerce/settings
POST   /api/woocommerce/test-connection
POST   /api/woocommerce/publish-product/:id (MOCKED)
DELETE /api/woocommerce/unpublish-product/:id
POST   /api/woocommerce/sync-inventory
```

## اللغات
- العربية (افتراضي)
- الفرنسية

## بيانات الاختبار
- البريد: test@test.com
- كلمة المرور: test123

## ملاحظات مهمة
- نظام SMS في وضع **المحاكاة (MOCKED)**
- WooCommerce APIs في وضع **المحاكاة (MOCKED)**
- إعدادات الطابعة وUSB في **واجهة فقط (UI only)**
- العملة: دينار جزائري (دج)

## المهام المستقبلية

### P1 - أولوية متوسطة
- ربط WooCommerce بمتجر حقيقي (يتطلب مفاتيح API)
- ربط خدمة SMS حقيقية (يتطلب اشتراك)
- ربط شركات الشحن الجزائرية (يتطلب عقود)

### P2 - أولوية منخفضة
- تفعيل الاختيار التلقائي لبطاقة SIM حسب رقم الهاتف
- تفعيل إعدادات الطابعة الفعلية
- طباعة ملصقات الباركود للطابعات الحرارية

## التغييرات الأخيرة (2026-02-09)

### تحسينات جديدة ✅
- **أيقونة تثبيت التطبيق (PWA)** - تظهر دائماً في الهيدر بجانب زر اللغة
- **منع البيع بدون حصة مفتوحة** - تحذير مرئي + رابط لفتح حصة جديدة
- **إضافة مورد جديد في الشراء** - زر سريع في نافذة الشراء الجديد
- **إصلاح validation حقول الأسعار** - استخدام toast بدلاً من رسائل المتصفح الافتراضية

### وحدة صيانة الهواتف النقالة ✅
تم إنشاء نظام متكامل لإدارة صيانة الهواتف يشمل:

#### الصفحات:
| المسار | الوصف |
|--------|-------|
| `/repairs` | تتبع الصيانة - قائمة الطلبات مع الإحصائيات والبحث والفلترة |
| `/repairs/new` | استقبال جهاز جديد - نموذج تسجيل كامل مع كل التفاصيل |
| `/repairs/parts` | قطع الغيار - إدارة مخزون قطع الغيار مع تنبيهات المخزون المنخفض |

#### APIs الصيانة:
```
GET    /api/repairs           - قائمة طلبات الصيانة
POST   /api/repairs           - إنشاء طلب جديد
GET    /api/repairs/stats     - إحصائيات الصيانة
GET    /api/repairs/{id}      - تفاصيل طلب
PUT    /api/repairs/{id}      - تحديث حالة الطلب
DELETE /api/repairs/{id}      - حذف طلب
```

#### APIs قطع الغيار:
```
GET    /api/spare-parts       - قائمة قطع الغيار
POST   /api/spare-parts       - إضافة قطعة جديدة
GET    /api/spare-parts/stats - إحصائيات المخزون
GET    /api/spare-parts/{id}  - تفاصيل قطعة
PUT    /api/spare-parts/{id}  - تحديث قطعة
DELETE /api/spare-parts/{id}  - حذف قطعة
```

#### حالات طلب الصيانة:
- `received` - مستلم
- `diagnosing` - قيد الفحص
- `waiting_parts` - بانتظار القطع
- `in_progress` - قيد الإصلاح
- `completed` - تم الإصلاح
- `delivered` - تم التسليم
- `cancelled` - ملغي

### الميزات السابقة (2026-02-07):
- ✅ تخصيص صفحة تسجيل الدخول (تم تفعيلها)
- ✅ صفحة الإحصائيات المتقدمة مع الرسوم البيانية
- ✅ صفحة الولاء والتسويق مع 3 تبويبات
- ✅ زر نشر/إلغاء نشر WooCommerce في تفاصيل المنتج
- ✅ **الوضع المظلم (Dark Mode)** - تبديل بين الوضع الفاتح والمظلم مع حفظ التفضيل
- ✅ **إضافة المخزون من الشراء فقط** - حقل الكمية محذوف من إضافة المنتج وللقراءة فقط في التعديل
- ✅ **صفحة الخدمات بالجملة (MCH-DZ Style)** - صفحة رئيسية للخدمات مع بطاقات ملونة وإحصائيات
- ✅ **خدمة فليكسي** - شحن رصيد الهاتف مع الكشف التلقائي عن المشغل
- ✅ **خدمة تعبئة أيدوم** - تعبئة ADSL و 4G LTE
- ✅ **خدمة البطاقات** - شراء وإدارة بطاقات التعبئة (موبيليس، جيزي، أوريدو)
- ✅ **صفحة كل العمليات** - سجل المعاملات مع الفلاتر والإحصائيات
- ✅ **صفحة نسب الأرباح** - جدول نسب الربح لكل خدمة ومشغل
- ✅ **صفحة التحويلات** - التحويلات الصادرة والواردة مع إمكانية إرسال تحويل جديد
- ✅ **صفحة دليل الهاتف** - إدارة جهات الاتصال مع الكشف التلقائي عن المشغل

### إصلاحات:
- ✅ إصلاح عرض السعر في صفحة المنتجات (retail_price بدلاً من price)
- ✅ إصلاح تحميل بيانات الإحصائيات (معالجة هياكل البيانات المتداخلة)
- ✅ إصلاح خطأ ObjectId في loyalty settings API
- ✅ ربط صفحات الصيانة بالـ Backend APIs (تم إزالة البيانات الثابتة)

## الملفات الجديدة
- `frontend/src/contexts/ThemeContext.js` - سياق إدارة الوضع المظلم
- `frontend/src/pages/RepairTrackingPage.js` - صفحة تتبع الصيانة
- `frontend/src/pages/RepairReceptionPage.js` - صفحة استقبال جهاز جديد
- `frontend/src/pages/SparePartsPage.js` - صفحة قطع الغيار
- `frontend/src/pages/ExpensesPage.js` - صفحة إدارة التكاليف والمصروفات

---

## التحديثات الأخيرة (2026-02-07) - الجلسة الحالية

### 6. نافذة الدفع المتقدم في نقطة البيع ✅ (2026-02-07)

#### الميزات:
- **زر "دفع متقدم"**: زر برتقالي بجانب زر تأكيد البيع
- **نافذة دفع احترافية** تحتوي على:
  - الإجمالي (Total)
  - حقل النقدي (Espèces) - لإدخال المبلغ المدفوع نقداً
  - حقل التحويل البنكي (Virement)
  - حقل الدين (Crédit) - يحسب تلقائياً الباقي
  - عرض المدفوع والباقي للدفع
- **الحساب التلقائي**: عند إدخال المبلغ النقدي، يتم حساب الدين تلقائياً
- **تسجيل الدين**: الباقي يسجل تلقائياً كدين على الزبون المختار
- **تحقق من الزبون**: رسالة تحذير إذا كان هناك دين بدون اختيار زبون

---

### 7. استيراد الجرد من Excel ✅ (2026-02-07)

#### الميزات الرئيسية:
- **تحميل ملف Excel**: دعم ملفات xlsx و xls
- **نظام 3 خطوات**:
  1. **تحميل الملف**: منطقة سحب وإفلات أو نقر للاختيار
  2. **تحديد الأعمدة**: ربط أعمدة Excel بالحقول (باركود، كمية، اسم)
  3. **معاينة وتطبيق**: عرض النتائج قبل التطبيق

#### التفاصيل:
- **الكشف التلقائي**: الأعمدة يتم التعرف عليها تلقائياً من الأسماء
- **معاينة البيانات**: عرض أول 5 صفوف مع تلوين الأعمدة المحددة
- **ملخص النتائج**: عدد المنتجات المتطابقة / غير الموجودة / الفروقات
- **جدول المعاينة**: عرض كامل مع الحالة والفرق لكل منتج
- **تحميل القالب**: زر لتحميل ملف Excel نموذجي

#### التنسيق المطلوب:
- عمود الباركود (إجباري)
- عمود الكمية (إجباري)
- عمود الاسم (اختياري - للعرض فقط)

---

### 4. ميزات جديدة في نقطة البيع (POS) ✅ (2026-02-07)

#### أ. المنتجات غير المتوفرة - بيع بالمخزون السالب ✅
- **خلفية متدرجة مميزة**: المنتجات ذات المخزون صفر تظهر بخلفية متدرجة (أصفر-برتقالي)
- **أيقونة تحذير**: أيقونة مثلث التحذير بدلاً من أيقونة الحزمة العادية
- **شارة "غير متوفر"**: شارة واضحة تظهر على المنتجات غير المتوفرة
- **المخزون باللون الأحمر**: عرض المخزون 0 بخلفية حمراء
- **إمكانية الإضافة للسلة**: يمكن إضافة المنتجات غير المتوفرة مع رسالة تحذير
- **المخزون السالب**: عند البيع يصبح المخزون سالب (-1، -2، إلخ)

#### ب. لوحة ديون الزبائن المحسنة ✅
- **عرض دائم**: اللوحة تظهر دائماً عند وجود ديون (وليس فقط عند الضغط على زر)
- **تصميم احترافي**: خلفية متدرجة مع أيقونات وألوان مميزة
- **تفاصيل شاملة**: اسم الزبون، رقم الهاتف، عدد الأيام منذ آخر شراء
- **مؤشر العجلة**: للديون العاجلة (أكثر من 30 يوم)
- **إجمالي الديون**: عرض المجموع الكلي للديون المستحقة
- **النقر للدفع**: النقر على الزبون يفتح نافذة دفع الدين مباشرة

### 5. إدارة القائمة السوداء من صفحة الزبائن ✅

#### الميزات:
- **مفتاح الفلترة**: تبديل "القائمة السوداء فقط" لعرض المحظورين
- **شارة العداد**: عرض عدد الزبائن المحظورين (مثلاً "3 محظور")
- **أزرار التحكم**: أيقونات Shield/ShieldOff للإضافة والإزالة من القائمة
- **نافذة حوار الحظر**: اختيار سبب الحظر من قائمة منسدلة
- **أسباب الحظر**: عدم الدفع، سلوك سيء، احتيال، إرجاع متكرر، أخرى
- **بطاقات مميزة**: الزبائن المحظورين تظهر بطاقاتهم بإطار أحمر وخلفية ملونة

#### ملاحظة:
- أزرار القائمة السوداء تظهر فقط للزبائن الذين لديهم أرقام هاتف
- القائمة السوداء مبنية على أرقام الهاتف وليس على الزبون نفسه

---

### 1. واجهة إعدادات البريد الإلكتروني ✅
تم إضافة tab جديد "البريد" في صفحة الإعدادات يشمل:
- تفعيل/تعطيل البريد الإلكتروني
- حقل مفتاح Resend API
- بريد المرسل واسم المرسل
- زر حفظ الإعدادات
- زر إرسال اختباري
- تعليمات الإعداد خطوة بخطوة

#### APIs البريد الإلكتروني:
```
GET  /api/email/settings      - جلب إعدادات البريد
PUT  /api/email/settings      - تحديث إعدادات البريد
POST /api/email/test          - إرسال بريد اختباري
POST /api/email/send-session-report - إرسال تقرير الحصة بالبريد
```

### 2. تنبيهات المصروفات المتكررة ✅
تم إضافة نظام تنبيهات للمصروفات المتكررة:
- عرض تنبيه برتقالي في أعلى صفحة المصروفات
- يظهر المصروفات المستحقة خلال أيام reminder_days_before
- زر "تم الدفع" لتسجيل الدفع وحساب الموعد التالي
- تنبيه أحمر عاجل عندما يكون الموعد اليوم أو غداً

#### APIs التنبيهات:
```
GET  /api/expenses/reminders            - جلب التنبيهات القادمة
POST /api/expenses/{id}/mark-paid       - تسجيل دفع وتحديث الموعد التالي
```

### 3. صفحة إدارة التكاليف والمصروفات ✅
صفحة `/expenses` تشمل:
- إحصائيات التكاليف (الإجمالي، هذا الشهر، الشهر الماضي، الفرق)
- التكاليف حسب الفئة (إيجار، فواتير، نقل، رواتب، مستلزمات، صيانة، أخرى)
- جدول المصروفات مع البحث والفلترة
- إضافة/تعديل/حذف المصروفات
- دعم المصروفات المتكررة (أسبوعي، شهري، سنوي)
- إعداد التذكير قبل الموعد (1، 3، 5، 7 أيام)

#### APIs المصروفات:
```
GET    /api/expenses         - جلب قائمة المصروفات
POST   /api/expenses         - إضافة مصروف جديد
PUT    /api/expenses/{id}    - تعديل مصروف
DELETE /api/expenses/{id}    - حذف مصروف
GET    /api/expenses/stats   - إحصائيات المصروفات
```

---

## التحديثات الأخيرة (2026-02-08) - الجلسة 3

### 1. نظام النسخ الاحتياطي الكامل ✅ جديد

#### الميزات:
- **تحميل نسخة احتياطية**: تحميل ملف JSON كامل للنظام
- **حفظ على السيرفر**: حفظ نسخة على السيرفر تلقائياً (آخر 10 نسخ)
- **استعادة من ملف**: رفع ملف نسخة واستعادة البيانات
- **قائمة النسخ**: عرض النسخ المحفوظة على السيرفر

#### APIs جديدة:
```
GET  /api/backup/create           - تحميل نسخة احتياطية
POST /api/backup/save-to-server   - حفظ على السيرفر
POST /api/backup/restore          - استعادة من ملف
GET  /api/backup/list             - قائمة النسخ المحفوظة
```

### 2. الحذف الانتقائي للبيانات ✅ جديد

#### الميزات:
- **اختيار أنواع البيانات**: المبيعات، المشتريات، الزبائن، الموردين، المنتجات، إلخ
- **رمز تأكيد**: يجب كتابة `DELETE-SELECTED` للتأكيد
- **تسجيل العملية**: حفظ سجل بمن حذف وماذا حذف

#### API:
```
POST /api/system/selective-delete
Body: { "data_types": ["sales", "customers"], "confirm_code": "DELETE-SELECTED" }
```

### 3. إنشاء حسابات للموظفين ✅ جديد

#### الميزات:
- **زر إنشاء حساب**: في بطاقة كل موظف
- **تعيين البريد وكلمة المرور والدور**
- **ربط الموظف بالحساب**
- **حذف الحساب**: إمكانية حذف حساب الموظف

#### APIs:
```
POST   /api/employees/{id}/create-account - إنشاء حساب
DELETE /api/employees/{id}/delete-account - حذف حساب
```

### 4. إدارة الإشعارات والتنبيهات ✅ جديد

#### الصفحة الجديدة: `/notifications`
- **تبويب الإشعارات**: عرض كل الإشعارات
- **تبويب الإعدادات**: التحكم في أنواع الإشعارات
- **إعدادات قابلة للتخصيص**:
  - تنبيه المخزون المنخفض (مع حد معين)
  - تذكير الديون (مع عدد الأيام)
  - فرق الصندوق (مع حد معين)
  - حالة الإصلاحات
  - الصوت
  - البريد الإلكتروني

#### APIs:
```
GET    /api/notifications/settings      - إعدادات الإشعارات
PUT    /api/notifications/settings      - تحديث الإعدادات
GET    /api/notifications/all           - كل الإشعارات
PUT    /api/notifications/mark-all-read - تحديد الكل كمقروء
DELETE /api/notifications/clear-all     - مسح الكل
```

### 5. عائلات الزبائن والموردين ✅ جديد

#### الميزات:
- **زر إضافة عائلة**: في نموذج إضافة الزبون والمورد
- **قائمة منسدلة**: لاختيار العائلة
- **إنشاء عائلة جديدة**: من نفس النموذج

#### الحقول المضافة:
- `family_id` في نموذج الزبون
- `family_id` في نموذج المورد

### 6. طرق عرض متعددة للمنتجات ✅ جديد

#### الأوضاع:
- **شبكة (Grid)**: العرض الافتراضي بالبطاقات
- **قائمة (List)**: عرض سطري مع تفاصيل
- **مضغوط (Compact)**: شبكة صغيرة للعرض السريع

#### الميزات:
- **أزرار التبديل**: في صفحة المنتجات
- **حفظ التفضيل**: في localStorage

### 7. ترتيب القائمة الجانبية يدوياً ✅ جديد

#### الصفحة الجديدة: `/settings/sidebar`
- **سحب وإفلات**: لتغيير ترتيب العناصر
- **إظهار/إخفاء**: مفتاح تبديل لكل عنصر
- **حفظ التفضيل**: في قاعدة البيانات لكل مستخدم
- **إعادة الترتيب الافتراضي**: زر لإعادة الترتيب الأصلي

#### المكتبات المستخدمة:
- `@dnd-kit/core`
- `@dnd-kit/sortable`
- `@dnd-kit/utilities`

#### APIs:
```
GET /api/settings/sidebar-order  - قراءة الترتيب
PUT /api/settings/sidebar-order  - حفظ الترتيب
```

---

## التحديثات الأخيرة (2026-02-08) - الجلسة 4

### إعادة هيكلة القائمة الجانبية ✅ جديد

#### الأقسام الرئيسية الجديدة:
تم نقل العناصر التالية لتصبح أقساماً رئيسية مستقلة:

| القسم | الأيقونة | العناصر الفرعية |
|-------|----------|-----------------|
| الرئيسية | LayoutDashboard | لوحة التحكم |
| المخزون | Package | المنتجات، عائلات المنتجات، المخازن، جرد المخزون، طباعة الباركود، تحديث الأسعار |
| المشتريات | ShoppingBag | المشتريات، الموردين، عائلات الموردين |
| **الزبائن** | Users | الزبائن، عائلات الزبائن، الموظفين |
| **المبيعات** | Receipt | نقطة البيع، حصص البيع، المبيعات، التكاليف، إدارة المال، ديون الزبائن، الديون |
| **التقارير** | BarChart3 | التقارير، إحصائيات متقدمة |
| **الإشعارات** | Bell | الإشعارات |
| **الولاء والتسويق** | Award | برنامج الولاء |
| الصيانة | Wrench | تتبع الصيانة، استقبال جهاز، قطع الغيار |
| الخدمات | Smartphone | فليكسي، أيدوم، بطاقات، عمليات، أرباح، تحويلات، دليل الهاتف |
| WooCommerce | Store | WooCommerce |
| التوصيل | Truck | شركات الشحن |
| الإدارة | Settings | المستخدمين، مفاتيح API، الإعدادات |

#### الملفات المعدلة:
- `frontend/src/components/Layout.js` - هيكل القائمة الجانبية
- `frontend/src/pages/SidebarSettingsPage.js` - الهيكل الافتراضي للترتيب

---

## المهام المعلقة

### P0 - أولوية عالية
- ⏳ تكوين SMTP/Resend الفعلي لإرسال البريد (يتطلب مفتاح API)

### P1 - أولوية متوسطة
- ربط WooCommerce بمتجر حقيقي
- ربط خدمة SMS حقيقية
- ربط WhatsApp Business API
- شحن رصيد EMERGENT_LLM_KEY للذكاء الاصطناعي

### P2 - أولوية منخفضة
- تفعيل إعدادات الطابعة الفعلية
- تحسين صفحة الخدمات بتصميم يشبه mch-dz.com

---

## التحديثات السابقة (2026-02-08) - الجلسة 2

### 1. المساعد الذكي (AI Assistant) ✅

#### الميزات:
- **زر AI في كل الصفحات**: زر بنفسجي ثابت في الزاوية السفلية اليسرى
- **لوحة المحادثة**: تفتح عند النقر على الزر
- **تبويب المحادثة**: محادثة مع الذكاء الاصطناعي
- **تبويب التحليل**: تحليلات سريعة (توقع المبيعات، اقتراحات التخزين، تحليل العملاء)
- **إجراءات سريعة**: أزرار للأسئلة الشائعة
- **سياق ذكي**: المساعد يتكيف حسب الصفحة الحالية

#### APIs جديدة:
```
POST   /api/ai/chat                    - محادثة مع AI
GET    /api/ai/chat-history/{session}  - تاريخ المحادثة
DELETE /api/ai/chat-history/{session}  - مسح المحادثة
POST   /api/ai/analyze                 - تحليل ذكي (sales_forecast, restock, customer_insights, product_description)
```

#### ملاحظات:
- يستخدم EMERGENT_LLM_KEY من .env
- قد يظهر خطأ "Budget exceeded" عند انتهاء الرصيد

### 2. إصلاح حفظ صور المنتجات ✅

#### المشكلة:
- الصور كانت تُرفع لكن لا تُحفظ في قاعدة البيانات

#### الحل:
- تصحيح `PurchasesPage.js` لإرسال `image_url` بدلاً من `image` (السطر 305)

### 3. تحسين حسابات الربح ✅

#### الميزات الجديدة:
- **تقرير الربح التفصيلي**: `/api/reports/profit-detailed`
  - تقسيم يومي للأرباح
  - أفضل المنتجات ربحاً
  - هامش الربح الفعلي
- **حساب الربح الفعلي في إغلاق الحصة**: بدلاً من التقدير 15%
- **تحسين DailySessionsPage.js**: حساب الربح من الفرق بين سعر البيع وسعر الشراء

#### API تقرير الربح التفصيلي:
```
GET /api/reports/profit-detailed?days=30

Response:
{
  "summary": {
    "total_revenue": float,
    "total_cost": float,
    "total_profit": float,
    "profit_margin": float,
    "avg_daily_profit": float,
    "period_days": int
  },
  "daily_breakdown": [...],
  "top_profitable_products": [...]
}
```

---

## ملاحظات فنية مهمة

### APIs في وضع المحاكاة (MOCKED):
- إرسال البريد الإلكتروني (يتطلب RESEND_API_KEY)
- إرسال رسائل WhatsApp (يتطلب Meta Business API)
- WooCommerce APIs
- SMS APIs

### بيانات الاختبار:
- البريد: admin@test.com
- كلمة المرور: admin123
- الدور: admin

### العملة:
- دينار جزائري (دج)

---

## التحديثات الأخيرة (2026-02-08)

### 1. تعديل أسعار المنتجات أثناء الشراء ✅

#### الميزات:
- **نافذة تعديل الأسعار**: تفتح عند النقر على أيقونة التعديل بجانب سعر الشراء
- **سعر الشراء الجديد**: حقل لإدخال السعر مع عرض نسبة التغيير (زيادة/نقصان)
- **هامش الربح**: أزرار سريعة (10%, 20%, 30%, 40%, 50%) لحساب الأسعار تلقائياً
- **أسعار الجملة والتجزئة**: تُحسب تلقائياً بناءً على الهامش
- **رفع صورة المنتج**: زر لإضافة أو تغيير صورة المنتج
- **خيار التحديث**: تحديث أسعار المنتج في قاعدة البيانات للمبيعات القادمة

#### الحسابات:
- سعر التجزئة = سعر الشراء × (1 + هامش%)
- سعر الجملة = سعر الشراء × (1 + هامش% × 70%)

#### APIs جديدة:
```
POST /api/upload/image - رفع صورة للمنتج
```

### 2. حذف رابط إنشاء حساب من صفحة تسجيل الدخول ✅
- إزالة رابط "إنشاء حساب" من صفحة تسجيل الدخول
- إضافة المستخدمين تتم فقط من إدارة المستخدمين أو رابط إحالة

### 3. تحسين صفحة جرد المخزون ✅
- **وضع التأكيد السريع**: المسح يؤكد الكمية الحالية مباشرة
- **مؤشر المنتج الأخير**: عرض آخر منتج تم مسحه
- **تبويبات الفلترة**: الكل / غير مجرود / فروقات
- **زر تأكيد الكمية**: تأكيد سريع للكمية الحالية
- **تصدير التقرير**: تصدير CSV
- **استيراد من Excel**: تحميل ملف Excel للجرد

### 4. تحسين صفحة تحديد الأسعار الجماعي ✅
- **أزرار النسب السريعة**: +5%, +10%, +15%, +20%, +25%, +30%
- **حاسبة هامش الربح**: تحديد الهامش وحساب الأسعار تلقائياً
- **خيارات متقدمة**: اختيار منتجات محددة، نطاق السعر
- **بحث في المعاينة**

### 5. استيراد الجرد من Excel ✅
- **نظام 3 خطوات**: تحميل → تحديد الأعمدة → معاينة وتطبيق
- **الكشف التلقائي** للأعمدة
- **معاينة البيانات** قبل التطبيق
- **تحميل قالب Excel** نموذجي



---

## التحديثات الأخيرة (2026-02-08) - الجلسة 5

### 1. أوضاع العرض والفرز للصفحات الرئيسية ✅ جديد

#### صفحة العملاء (CustomersPage.js):
- **وضع الشبكة (Grid)**: عرض البطاقات التقليدي
- **وضع القائمة (List)**: جدول مفصل بالأعمدة:
  - الاسم، الهاتف، النوع، إجمالي المشتريات، الرصيد، آخر زيارة، الإجراءات
- **الفرز**: الاسم، الرصيد، المشتريات، التاريخ
- **اتجاه الفرز**: تصاعدي / تنازلي
- **حفظ التفضيل**: في localStorage

#### صفحة الموردين (SuppliersPage.js):
- **وضع الشبكة (Grid)**: بطاقات مع زر الدفع المتقدم
- **وضع القائمة (List)**: جدول مفصل بالأعمدة:
  - الاسم، الهاتف، البريد، إجمالي المشتريات، الرصيد، الرصيد المتقدم، الإجراءات
- **الفرز**: الاسم، الرصيد، المشتريات، الرصيد المتقدم
- **اتجاه الفرز**: تصاعدي / تنازلي
- **حفظ التفضيل**: في localStorage

#### صفحة المنتجات (ProductsPage.js):
- **الفرز الجديد**: الاسم، السعر، المخزون، سعر الشراء
- **اتجاه الفرز**: تصاعدي / تنازلي
- **أوضاع العرض موجودة سابقاً**: شبكة، قائمة، مضغوط

#### ملاحظات تقنية:
- استخدام `sortedCustomers`، `sortedSuppliers`، `sortedProducts` للعرض
- الفرز يتم على جانب العميل (client-side)
- التفضيلات محفوظة في localStorage

---

## التحديثات الأخيرة (2026-02-08) - الجلسة 6 (إكمال جميع المهام)

### 1. اختيار المستودع في نقطة البيع ✅
- إضافة قائمة منسدلة لاختيار المستودع في صفحة POS
- المستودع الرئيسي يُحدد تلقائياً
- يُرسل `warehouse_id` مع بيانات البيع

### 2. سجل تغييرات الأسعار ✅
- صفحة جديدة `/price-history`
- تسجيل تلقائي لأي تغيير في الأسعار (شراء، جملة، تجزئة)
- إحصائيات: إجمالي التغييرات، الزيادات، الانخفاضات، متوسط التغيير
- فلترة حسب نوع السعر
- APIs: `GET /api/price-history`, `GET /api/products/{id}/price-history`

### 3. التقارير الذكية ✅
- صفحة جديدة `/smart-reports`
- إعداد إرسال تقارير يومية بالبريد الإلكتروني
- محتوى التقرير: ملخص المبيعات، تنبيهات المخزون، نصائح AI
- معاينة التقرير قبل الإرسال
- APIs: `/api/smart-reports/settings`, `/api/smart-reports/preview`, `/api/smart-reports/send-now`

### 4. تنبيهات الموظفين ✅
- صفحة جديدة `/employee-alerts`
- قسم جديد في القائمة الجانبية: "إدارة الموظفين"
- تحديد حدود الخصم والدين لكل موظف
- تنبيهات عند اقتراب الموظف من الحد المسموح
- APIs: `/api/employees/alerts/active`, `/api/employees/{id}/alert-settings`

---

## الميزات المكتملة (Summary)

| الميزة | الحالة | الملفات |
|--------|--------|---------|
| أوضاع العرض للعملاء | ✅ | CustomersPage.js |
| أوضاع العرض للموردين | ✅ | SuppliersPage.js |
| الفرز للمنتجات | ✅ | ProductsPage.js |
| اختيار المستودع في POS | ✅ | POSPage.js |
| سجل تغييرات الأسعار | ✅ | PriceHistoryPage.js, server.py |
| التقارير الذكية | ✅ | SmartReportsPage.js, server.py |
| تنبيهات الموظفين | ✅ | EmployeeAlertsPage.js, server.py |
| **سجل شراء المنتج** | ✅ | ProductDetailPage.js, server.py |
| **تحذير السعر** | ✅ | ProductDetailPage.js (Lines 111-129) |
| **خيارات إيصال POS** | ✅ | POSPage.js, SettingsPage.js |
| **سجل مبيعات POS** | ✅ | POSPage.js (Previous Sales Dialog) |
| **عرض/إخفاء كلمة المرور** | ✅ | LoginPage, RegisterPage, TenantLoginPage, SettingsPage, SaasAdminPage |
| **تصنيف المشترك** | ✅ | SaasAdminPage.js (retailer, wholesaler, distributor) |
| **إدارة المستخدمين الكاملة** | ✅ | SettingsPage.js (Edit, Delete, Permissions) |
| **القائمة الجانبية الكاملة** | ✅ | AuthContext.js, Layout.js (isAdmin includes super_admin) |
| **نظام الأدوار المتقدم** | ✅ | server.py (9 أدوار), SettingsPage.js (9 أدوار في الواجهة) |
| **إدارة الموظفين الكاملة** | ✅ | EmployeesPage.js (إحصائيات، جدول رواتب، حضور) |
| **التقارير الذكية** | ✅ | SmartReportsPage.js (نصائح AI، جدولة، إرسال) |
| **WooCommerce** | ✅ | WooCommercePage.js (تكامل، مزامنة منتجات) |

---

## الأدوار والصلاحيات المتقدمة (تحديث 2026-02-08)

### الأدوار المتوفرة:
1. **سوبر أدمين (super_admin)** - صلاحيات كاملة على النظام بالكامل + إدارة SaaS
2. **مدير (admin)** - صلاحيات كاملة على المتجر
3. **مشرف (manager)** - إدارة العمليات اليومية
4. **مشرف مبيعات (sales_supervisor)** - إشراف على المبيعات والعملاء فقط
5. **بائع (seller)** - عمليات البيع الأساسية فقط
6. **مدير مخزون (inventory_manager)** - إدارة المخزون والمشتريات فقط
7. **مسؤول متجر إلكتروني (ecommerce_manager)** - إدارة المتجر الإلكتروني
8. **محاسب (accountant)** - التقارير المالية والديون والمصاريف
9. **مستخدم عادي (user)** - عرض فقط

### تصنيفات الصلاحيات:
- **عمليات البيع**: نقطة البيع، المبيعات، الزبائن، الديون
- **عمليات المخزون**: المنتجات، المخزون، المشتريات، الموردين
- **شؤون الموظفين**: الموظفين
- **العمليات المالية**: المصاريف، التقارير
- **إدارة النظام**: الإعدادات، المستخدمين، API Keys
- **الخدمات**: WooCommerce، التوصيل، الولاء، الإشعارات

### صلاحيات متقدمة جديدة:
- `price_change` - تغيير الأسعار
- `stock_adjust` - تعديل المخزون
- `refund` - استرجاع المبيعات
- `discount` - تطبيق خصومات
- `credit` - البيع بالآجل
- `blacklist` - إضافة للقائمة السوداء
- `transfer` - نقل المخزون
- `count` - جرد المخزون
- `approve` - موافقة على العمليات

---

## المهام المستقبلية (Backlog)

1. ربط WooCommerce بمتجر حقيقي
2. ربط خدمة SMS حقيقية
3. تفعيل WhatsApp Business API
4. ميزة القائمة السوداء للعملاء (تحسين UI)
5. إضافة أوضاع عرض إضافية لباقي الصفحات

---

## تحديث 2026-02-08 - الميزات الجديدة المُنفذة

### 1. سجل شراء المنتج (Product Purchase History)
- **الموقع:** صفحة تفاصيل المنتج `/products/{id}`
- **المميزات:**
  - عرض سجل المشتريات مع معلومات المورد
  - عرض سجل المبيعات
  - عرض سجل تغييرات الأسعار
  - 3 تبويبات: المشتريات / المبيعات / الأسعار
- **APIs:** 
  - `GET /api/products/{id}/purchase-history`
  - `GET /api/products/{id}/sales-history`
  - `GET /api/price-history`

### 2. تحذير السعر (Price Warning)
- **الموقع:** صفحة تفاصيل المنتج
- **المميزات:**
  - تحذير أحمر إذا كان سعر التجزئة < سعر الشراء
  - تحذير إذا كان سعر الجملة < سعر الشراء
  - تحذير إذا كان سعر نصف الجملة < سعر الشراء

### 3. خيارات إيصال POS (Receipt Options)
- **الموقع:** صفحة الإعدادات `/settings` → تبويب الطابعة
- **المميزات:**
  - طباعة تلقائية بعد البيع (Toggle)
  - عرض حوار الطباعة (Toggle)
  - اختيار قالب الإيصال الافتراضي
  - 3 قوالب: حراري 58mm / حراري 80mm / A4
  - تخصيص الشعار والترويسة والتذييل لكل قالب
  - نص تذييل مخصص
- **APIs:**
  - `GET /api/settings/receipt`
  - `POST /api/settings/receipt`

### 4. سجل مبيعات POS (Sales History)
- **الموقع:** صفحة POS `/pos`
- **المميزات:**
  - زر سجل المبيعات (أيقونة History بنفسجية)
  - حوار عرض المبيعات السابقة
  - فلاتر الفترة: اليوم / الأسبوع / الشهر / مخصص
  - إحصائيات: عدد المبيعات / إجمالي المبيعات / المبيعات بالدين
  - جدول المبيعات مع الإجراءات (عرض / طباعة)
  - عرض تفاصيل كل عملية بيع
- **APIs:**
  - `GET /api/sales?start_date=&end_date=`
  - `GET /api/sales/{id}`
  - `GET /api/sales/{id}/invoice-pdf`

### 4. إضافة مستخدم جديد ✅ جديد (2026-02-09)
- **زر "إضافة مستخدم"** في صفحة المستخدمين
- **حوار إضافة مستخدم** مع حقول:
  - الاسم
  - البريد الإلكتروني
  - كلمة المرور (مع إظهار/إخفاء)
  - الدور (9 أدوار: سوبر أدمين، مدير، مدير فرع، بائع، محاسب، مدير مخزون، مندوب توصيل، فني صيانة، مستخدم)
- **API**: `POST /api/users` (محمي للمدراء فقط)

### 5. التقارير المالية الشاملة ✅ جديد (2026-02-09)
- **تبويب جديد** "التقارير المالية" في لوحة SaaS
- **إحصائيات الإيرادات**:
  - إجمالي الإيرادات
  - إيراد الشهر
  - إيراد السنة
  - المصاريف (تقديري 30%)
  - صافي الربح
  - مدفوعات معلقة
- **طرق الدفع**:
  - نقدي (CCP/Baridimob)
  - تحويل بنكي
  - Stripe
  - نقدي/يدوي
- **فلترة بالتاريخ**: كل الوقت، اليوم، هذا الأسبوع، هذا الشهر، هذه السنة
- **قسم حساب الأرباح الصافية** مع تفصيل كامل
- **API**: `GET /api/saas/finance-reports` (محمي للسوبر أدمين فقط)

---

## التحديثات الأخيرة (2026-02-09) - إعادة هيكلة الشريط الجانبي

### التعديلات المُنجزة:

#### 1. نقل "المستخدمين" و"الصلاحيات" إلى قسم "إدارة الموظفين" ✅
- **المستخدمين** (`/users`) - أُضيفت لقسم إدارة الموظفين
- **الصلاحيات** (`/permissions`) - أُضيفت لقسم إدارة الموظفين
- تظهر للمدراء (admin) و super_admin فقط

#### 2. إزالة "العمال" من قائمة "الزبائن" ✅
- قسم الزبائن يحتوي الآن فقط على: الزبائن، عائلات الزبائن

#### 3. إعادة تنظيم قسم "الإدارة" ✅
- قسم **"الإدارة"** يحتوي الآن فقط على: **مميزات البرنامج** (`/features`)
- تم إنشاء قسم جديد **"الإعدادات"** يحتوي على:
  - مفاتيح API (`/api-keys`)
  - الإعدادات (`/settings`)
  - ترتيب القائمة (`/settings/sidebar`)

#### 4. إصلاح مشكلة تسجيل الدخول ✅
- تم تحديث كلمات مرور جميع المستخدمين في قاعدة البيانات
- كلمة المرور الافتراضية لجميع الحسابات: `password`

### هيكل الشريط الجانبي الجديد:
| القسم | العناصر |
|-------|---------|
| الرئيسية | لوحة التحكم |
| المخزون | المنتجات، عائلات المنتجات، المخازن، ... |
| المشتريات | المشتريات، الموردين، عائلات الموردين |
| الزبائن | الزبائن، عائلات الزبائن |
| المبيعات | نقطة البيع، المبيعات، التكاليف، ... |
| التقارير | التقارير، الإحصائيات |
| الإشعارات | الإشعارات |
| الولاء والتسويق | برنامج الولاء |
| **إدارة الموظفين** | الموظفين، تنبيهات الحدود، **المستخدمين**، **الصلاحيات** |
| الصيانة | تتبع الصيانة، استقبال جهاز، قطع الغيار |
| الخدمات | فليكسي، أيدوم، بطاقات، ... |
| WooCommerce | WooCommerce |
| التوصيل | شركات الشحن |
| **الإعدادات** | مفاتيح API، الإعدادات، ترتيب القائمة |
| **الإدارة** | **مميزات البرنامج** فقط |
| NT Commerce | لوحة تحكم SaaS (super_admin فقط) |

### بيانات الاختبار:
- **Super Admin**: super@ntcommerce.com / password
- **Admin**: admin@test.com / password
- **المدير**: manager@test.com / password
- **البائع**: seller@test.com / password

---

## نظام NT Commerce SaaS (جديد - 2026-02-08)

### ما تم بناؤه:

**1. صفحة Landing احترافية** `/landing`
- عرض المميزات والخدمات
- قسم الأسعار مع تبديل (شهري/6 أشهر/سنوي)
- آراء العملاء
- دعوة للعمل (CTA)
- تصميم متجاوب

**2. نظام التسجيل والخطط** `/register`
- خطوتين: اختيار الخطة → البيانات
- 14 يوم تجربة مجانية
- إنشاء قاعدة بيانات منفصلة لكل مشترك

**3. تسجيل دخول المشتركين** `/tenant-login`
- تسجيل دخول خاص بالمشتركين
- التحقق من صلاحية الاشتراك

**4. لوحة تحكم Super Admin** `/saas-admin`
- إحصائيات شاملة
- إدارة المشتركين (إضافة/تعديل/تعطيل/حذف)
- إدارة الخطط والأسعار
- تمديد الاشتراكات
- سجل المدفوعات

### الخطط المُنشأة:
| الخطة | شهري | 6 أشهر | سنوي | المنتجات | المستخدمين |
|-------|------|--------|------|----------|-----------|
| المبتدئ | 5,000 | 25,500 | 45,000 | 100 | 2 |
| الاحترافي | 12,000 | 61,200 | 108,000 | 500 | 5 |
| المؤسسات | 25,000 | 127,500 | 225,000 | غير محدود | غير محدود |

### بيانات الدخول:
- **Super Admin:** super@ntcommerce.com / superadmin123
- **Admin العادي:** admin@test.com / admin123

### الملفات الجديدة:
- `/app/frontend/src/pages/landing/LandingPage.js`
- `/app/frontend/src/pages/landing/RegisterPage.js`
- `/app/frontend/src/pages/landing/TenantLoginPage.js`
- `/app/frontend/src/pages/admin/SaasAdminPage.js`

### APIs الجديدة:
- `GET/POST /api/saas/plans` - إدارة الخطط
- `GET/POST /api/saas/tenants` - إدارة المشتركين
- `POST /api/saas/register` - تسجيل مشترك جديد
- `POST /api/saas/tenant-login` - تسجيل دخول المشتركين
- `POST /api/saas/tenants/{id}/extend-subscription` - تمديد الاشتراك
- `GET /api/saas/stats` - إحصائيات لوحة التحكم

---

## التحديثات الأخيرة (2026-02-09) - إصلاح الأخطاء

### الأخطاء التي تم إصلاحها:

#### 1. ✅ إصلاح انهيار صفحة البيع (POS) - P0
- **المشكلة الأصلية**: "Objects are not valid as a React child" عند تأكيد البيع
- **الحالة**: تم التحقق - لم تكن هناك مشكلة في الكود، الـ API يعمل بشكل صحيح
- **الاختبار**: عملية البيع تتم بنجاح مع رقم الفاتورة

#### 2. ✅ إصلاح أزرار فتح الجلسة - P1
- **المشكلة الأصلية**: أزرار فتح/غلق الجلسة لا تعمل من الواجهة
- **الحالة**: تعمل بشكل صحيح

#### 3. ✅ إصلاح الترجمة الفرنسية لصناديق النقد - P2
- **المشكلة الأصلية**: أسماء صناديق النقد تظهر بالعربية في الوضع الفرنسي
- **الإصلاح**:
  - تحديث `CashBoxResponse` في `server.py` لإضافة حقل `name_fr`
  - تحديث `init_cash_boxes()` لإضافة `name_fr` للصناديق الموجودة
  - تحديث `DashboardPage.js` لعرض `name_fr` عند اللغة الفرنسية
  - تحديث `CashManagementPage.js` لعرض `name_fr` في القوائم المنسدلة
- **الأسماء الفرنسية**: Caisse, Compte bancaire, Portefeuille électronique, Coffre-fort

### الملفات المُعدلة:
- `/app/backend/server.py` - إضافة `name_fr` لـ CashBoxResponse وتحديث init_cash_boxes
- `/app/frontend/src/pages/DashboardPage.js` - عرض الاسم الفرنسي لصناديق النقد
- `/app/frontend/src/pages/CashManagementPage.js` - عرض الاسم الفرنسي في 3 مواقع

### نتائج الاختبار:
- Backend: 100% نجاح (8/8 اختبارات)
- Frontend: 100% نجاح (جميع التدفقات تعمل)
- Test Report: `/app/test_reports/iteration_29.json`

